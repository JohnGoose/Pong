#pragma once

const float BallRadius = 16.f;
const std::size_t BallPoints = 32;
const float FiringSpeed = 300;
const float PaddleWidth = 10;
const float PaddleHeight = 100;
const float PaddleOffsetFromEdge = 20;
const float PaddleMoveSpeed = 300;
const float WindowWidth = 1024;
const float WindowHeight = 720;
const float SplashStateTimer = 4.0;

enum Side
{
    LEFT = 0,
    RIGHT
};
